import{HttpErrorResponse,HttpInterceptorFn}from'@angular/common/http';import{inject}from'@angular/core';import{Router}from'@angular/router';import{catchError,throwError}from'rxjs';import{environment}from'../../environments/environment';import{AuthService}from'./auth.service';import{ToastService}from'./toast.service';export const authInterceptor:HttpInterceptorFn=(req,next)=>{const a=inject(AuthService),r=inject(Router),t=inject(ToastService),token=a.token();const request=token&&req.url.startsWith(environment.apiUrl)?req.clone({setHeaders:{Authorization:`Bearer ${token}`}}):req;return next(request).pipe(catchError((e:HttpErrorResponse)=>{if(e.status===401&&token){a.logout();t.show('Tu sesión venció. Volvé a ingresar.','err');r.navigate(['/login'])}else if(e.status===403)t.show('No tenés permisos para realizar esta acción.','err');return throwError(()=>e)}))};
