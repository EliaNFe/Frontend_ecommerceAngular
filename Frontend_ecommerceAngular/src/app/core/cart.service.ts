import{Injectable,computed,signal}from'@angular/core';import{CartItem,Product}from'./models';const KEY='sotobosque_cart';@Injectable({providedIn:'root'})export class CartService{readonly items=signal<CartItem[]>(this.load());readonly count=computed(()=>this.items().reduce((n,i)=>n+i.cantidad,0));readonly total=computed(()=>this.items().reduce((n,i)=>n+i.product.precio*i.cantidad,0));add(p:Product,q=1){if(p.stock<1)return;const a=[...this.items()],i=a.findIndex(x=>x.product.id===p.id);i>=0?a[i]={product:p,cantidad:Math.min(p.stock,a[i].cantidad+q)}:a.push({product:p,cantidad:Math.min(p.stock,q)});this.save(a)}set(id:number,q:number){this.save(this.items().map(i=>i.product.id===id?{...i,cantidad:Math.max(1,Math.min(i.product.stock,q))}:i))}remove(id:number){this.save(this.items().filter(i=>i.product.id!==id))}clear(){this.save([])}private save(v:CartItem[]){this.items.set(v);localStorage.setItem(KEY,JSON.stringify(v))}private load():CartItem[]{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}}}
